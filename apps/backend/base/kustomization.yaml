apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: env-config
    literals: 
      - operation=""
      # - operation="add"
      # - operation="mul"
commonLabels:
  app: backend
