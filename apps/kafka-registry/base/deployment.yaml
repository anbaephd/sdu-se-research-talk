apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry
spec:
  selector:
    matchLabels:
      app: registry
  template:
    metadata:
      labels:
        app: registry
    spec:
      containers:
      - name: schema-registry
        image: confluentinc/cp-schema-registry:7.8.0
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8081
          protocol: TCP
        env:
        - name: DEMOOPS
          valueFrom:
            configMapKeyRef:
              name: env-config
              key: operation